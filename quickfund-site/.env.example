# --- server-side only ---
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-sonnet-4-6
# Optional: POSTs each lead as JSON (Zapier/Make/Apps Script -> Sheet/CRM).
LEAD_WEBHOOK_URL=
# Set to 1 to test the full pipeline without an API key (canned responses).
AI_MOCK=
# Long random string; enables the /admin lead viewer. Leave empty to disable.
ADMIN_TOKEN=

# --- public (browser-visible) ---
NEXT_PUBLIC_GA4_ID=
NEXT_PUBLIC_META_PIXEL_ID=
