import AdminLeads from "@/components/pages/AdminLeads";

export const metadata = {
  title: "Leads — QuickFund Admin",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <AdminLeads />;
}
