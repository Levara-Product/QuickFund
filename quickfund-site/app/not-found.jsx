import NotFound from "@/components/pages/NotFound";

export const metadata = { title: "Page not found — QuickFund", robots: { index: false } };
export default function NotFoundPage() {
  return <NotFound />;
}
